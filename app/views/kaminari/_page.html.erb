<li class="page-item <%= "active" if page.current? %>">
  <% if page.current? %>
    <span class="page-link"><%= page %></span>
  <% else %>
    <%= link_to page, url, class: "page-link", remote: remote, rel: page.rel %>
  <% end %>
</li>
