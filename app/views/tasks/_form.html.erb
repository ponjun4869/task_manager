<% if @task.errors.any? %>
  <div class="alert alert-danger">
    <h2 class="h6"><%= @task.errors.count %>件のエラーがあります</h2>

    <ul class="mb-0">
      <%# 保存に失敗した理由を1件ずつ表示する %>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%# new画面とedit画面で共通利用するタスクフォーム %>
<%= form_with model: @task do |f| %>

  <div class="mb-3">
    <%= f.label :title, "タイトル", class: "form-label" %>
    <%= f.text_field :title, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :content, "内容", class: "form-label" %>
    <%= f.text_area :content, rows: 6, class: "form-control" %>
  </div>

  <div class="mb-4">
    <%= f.label :status, "ステータス", class: "form-label" %>
    <%# Taskモデルで定義したステータス選択肢を表示する %>
    <%= f.select :status, Task.status_options, {}, class: "form-select" %>
  </div>

  <div class="mb-4">
    <%= f.label :priority, "優先度", class: "form-label" %>
    <%# Taskモデルで定義した優先度選択肢を表示する %>
    <%= f.select :priority, Task.priority_options, {}, class: "form-select" %>
  </div>

  <div class="mb-4">
    <%= f.label :deadline, "期限日", class: "form-label" %>
    <%= f.date_field :deadline, class: "form-control" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "戻る", tasks_path, class: "btn btn-outline-secondary" %>
  </div>

<% end %>
